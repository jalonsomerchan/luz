window.LUZ_CONFIG = {
  apiBase: 'https://alon.one/api/luz',
  API_BASE_URL: 'https://alon.one/api/luz'

  // Si tu API usa rutas concretas, puedes forzarlas aquí sin tocar el código:
  // dayEndpoints: [
  //   { path: 'precios', params: { fecha: '{date}' } },
  //   { path: 'dia', params: { date: '{date}' } }
  // ]
};
